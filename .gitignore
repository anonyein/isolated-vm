# clangd cache
**/dist
**/node_modules
# Javet .so unpacked from libs.7z (each ~140MB, exceeds GitHub's 100MB limit)
/libs/
/deps/javet/
/.cache
/.env
/.pnpm-store
/build
/compile_commands.json

/.xmake/
*.pcm
